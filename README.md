# postgres_backup

## Установка:

```
curl -o ~/backup.sh https://raw.githubusercontent.com/potesuch/postgres_backup/main/backup.sh && chmod +x ~/backup.sh && ~/backup.sh
```

## Команды:

- `postgres-backup` — быстрый доступ в меню из любой точки системы
